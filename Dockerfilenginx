FROM nginx:latest

RUN chmod -R 777 /var/cache/nginx/ \
        && touch /var/run/nginx.pid \
        && chmod a+w /var/run/nginx.pid && chown root:root nginx && chmod 755 nginx && chmod u+s nginx

COPY default.conf /etc/nginx/conf.d/
EXPOSE 8080
USER nginx
